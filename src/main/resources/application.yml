spring:
  application:
    name: sistema-matricula-utp
  sql:
    init:
      mode: always
      continue-on-error: true
  profiles:
    active: local
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
